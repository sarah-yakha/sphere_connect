import React from 'react'
import { Login } from '../form/Login'
import { Routing } from '../../routes'

export const HomePage = () => {
  return (
    <div> 
    HEllo world!
    <Routing />
    </div>
  )
}
